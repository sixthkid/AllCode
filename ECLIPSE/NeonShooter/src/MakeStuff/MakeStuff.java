package MakeStuff;
public class MakeStuff {
	MakeComponents components;
	MakeVisuals visuals;
	SetComponents setComponents;
	SetVisuals setVisuals;
	public MakeStuff() {
		components = new MakeComponents();
		visuals = new MakeVisuals();
		setComponents = new SetComponents();
		setVisuals = new SetVisuals();
	}	
}